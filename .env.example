# Base URL of the REST API (see docs/openapi.yaml)
NEXT_PUBLIC_API_BASE_URL=https://frontend-task-chatapp.onrender.com/api

# Socket.IO server URL — the host root, not the /api base above; the
# server mounts the socket separately from the REST API
NEXT_PUBLIC_SOCKET_URL=https://frontend-task-chatapp.onrender.com
